---
one:
  coverage_history: recent
  authority: bathurst

two:
  coverage_history: recent
  authority: bayside

previous:
  coverage_history: previous
  authority: bathurst

# Older data
older:
  coverage_history: older
  authority: bayside

# Historical data
historical:
  coverage_history: historical
  authority: bathurst

# Consecutive identical records for testing optimization
identical1:
  coverage_history: identical1
  authority: bayside

identical2:
  coverage_history: identical2
  authority: bayside

identical3:
  coverage_history: identical3
  authority: bayside